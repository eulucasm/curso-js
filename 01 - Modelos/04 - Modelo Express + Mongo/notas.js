/* 
Passos executados:

1º- separamos as rotas, criando um arquivo chamado routes
importamos o express, criamos uma variavel de rotas.
o router é o cara responsável por tratar as rotas,

2º- Criamos a controller onde ficam os nossos as funções de controle mesmo
e passamos a referencia para o nosso arquivo de rotas.

3º- incluímos no server a referencia ao arquivo de rotas.

4º- criamos a pasta de views, fizemos a referencia no nosso arquivo server,
incluímos o EJS para renderizar a pagina, criamos a pasta de views e enviamos
o arquivo index.ejs, referenciando ele na nossa homeController.

5º- criamos a base de dados no mongo atlas, usando uma base free na nuvem para teste.
configuramos essa base na nossa server.js

6º- Padrao de projeto utilizado aqui = MVC -> Models, views e controllers


*/