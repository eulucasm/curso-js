//string, number, undefined, null, boolean, symbol

const nome = 'Lucas'; //string
const nome1 = 'Matheus'; //string
const nome2 = 'Maria'; //string
const num1 = 10; //number
const num2 = 100; //number
let nomeAluno; //undefined -> não aponta local nenhum na memoria
const sobrenomeAluno = null; //nulo -> não aponta local nenhum na memoria
const boolean; // true or false -> ou um ou outro.(valor lógico).